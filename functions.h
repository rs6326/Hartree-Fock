#ifndef functions_h
#define functions_h
#include <iostream>
#include <fstream>
#include <cmath>
#include <vector>


typedef std::vector<std::vector<double>> twoD_vec;

int count_lines( const char* filename); //count the number of lines in a file

        //not actually sure I want to do this... does making an HF class make sense?


#endif